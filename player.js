/*function Player() {
    this.pos = createVector(x, y);
    this.vel = createVector(0, 0);
    this.acc = createVector(0, 0);
    this.width = width;
    this.height = height;
    this.spr = createSprite(this.pos.x, this.pos.y, this.width, this.height);

    var update = function() {
        this.vel.add(this.acc);
        this.pos.add(this.vel;
        this.acc.mul(0);
    }

}
*/